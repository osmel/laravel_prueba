        //$producto = Producto::first();
        //$producto= Producto::all(); //->take(10); 

        /*
        $producto= Producto::orderBy('id')->chunk(1, function ($prod) { //bloques de 100 registros
            foreach ($prod as $p) {
                print_r($p);

                //return false;  //para parar el proceso del bloque
            }
        });
        */
        //::where('fabricante_id', 1)->first()->foto[0]->url;
        //dd("ad");
        //dd($producto);

        //dd($producto[0]->foto[0]->url);
        //dd($producto[0]->foto[0]->url);